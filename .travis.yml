language: node_js
node_js:
  - 0.12
script: cd frontend && npm install -g npm && npm install && npm run travis
before_install:
    - export DISPLAY=:99.0
    - sh -e /etc/init.d/xvfb start
branches:
  only:
    - master
